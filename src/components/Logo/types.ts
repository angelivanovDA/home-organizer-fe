export interface LogoProps {
  showTitle?: boolean;
  isAuth?: boolean;
}

export interface LogoImageProps {
  className?: string;
  /** When true, image is decorative (empty alt). */
  decorative?: boolean;
}
